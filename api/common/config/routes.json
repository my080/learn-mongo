{
  "routes": [
    {
      "method": "POST",
      "path": "/login",
      "handler": "Common.login",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/send-email",
      "handler": "Common.sendEmail",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/logout",
      "handler": "Common.logout",
      "config": {
        "policies": []
      }
    }
  ]
}
