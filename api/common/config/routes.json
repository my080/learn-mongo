{
  "routes": [
    {
      "method": "POST",
      "path": "/login",
      "handler": "Common.login",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/logout",
      "handler": "Common.logout",
      "config": {
        "policies": []
      }
    }
  ]
}
